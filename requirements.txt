# ============================================================
# AIC Video Retrieval System — Python Dependencies
# ============================================================

# --- Core Scientific Stack ---
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0

# --- Vector Index & Search ---
faiss-cpu>=1.8.0          # Use faiss-gpu on Kaggle/CUDA env
qdrant-client>=1.9.0

# --- Machine Learning / Embeddings ---
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.44.0
open-clip-torch>=2.26.1   # OpenCLIP: CLIP-32, CLIP-L/14
sentence-transformers>=3.0.0  # BGE-M3, multilingual-e5

# --- OCR ---
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# --- ASR ---
faster-whisper>=1.0.0     # Faster-Whisper (CTranslate2 backend)

# --- Object Detection ---
ultralytics>=8.2.0        # YOLOv11

# --- VLM / LLM ---
# Qwen2.5-VL: loaded via transformers + qwen_vl_utils
qwen-vl-utils>=0.0.8
accelerate>=0.34.0
bitsandbytes>=0.43.0      # 4-bit / 8-bit quantization for VLM

# --- Video / Image Processing ---
opencv-python-headless>=4.10.0
Pillow>=10.4.0
imageio>=2.34.0
av>=12.0.0                # PyAV: fast video frame reading

# --- Data Storage & Serialization ---
pyarrow>=16.0.0           # Parquet read/write
h5py>=3.11.0              # HDF5 embedding cache
sqlite-utils>=3.36.0

# --- Configuration ---
PyYAML>=6.0.1
omegaconf>=2.3.0

# --- API & Web ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0

# --- Logging & Monitoring ---
loguru>=0.7.2
rich>=13.7.0              # Pretty terminal output
tqdm>=4.66.0

# --- Utilities ---
python-dotenv>=1.0.1
click>=8.1.7              # CLI
kaggle>=1.6.12            # Kaggle API client
